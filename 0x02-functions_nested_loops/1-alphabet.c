#include "main.h"

/**
 * print_alphabet - prints the alphabet in lower case and new line
 */
void print_alphabet(void)
{
	char letter;

	for (letter = 'a'a; letter <= 'z'; letter++)
		_putchar(letter);

	_putchar('\n');
}
