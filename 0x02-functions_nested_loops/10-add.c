#include "main.h"

/**
 * add - Adds two integers and returns the result
 *
 * @x: number one.
 * @y: number two.
 *
 * Return: Addition of number one and two
 */
int add(int x, int y)
{
	return (x + y);
}
